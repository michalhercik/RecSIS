package courses

import (
    "github.com/michalhercik/RecSIS/utils"
    "github.com/michalhercik/RecSIS/database"
)

templ Content(data *database.CoursesData) {
    <form>
        <input type="text"/>
    </form>
}

templ Page(data *database.CoursesData) {
    @utils.Page("Courses") {
        @Content(data)
    }
}