package courses

import (
    "github.com/michalhercik/RecSIS/utils"
    "github.com/michalhercik/RecSIS/mock_data"
)

templ Content(data *[]mock_data.Course) {
    <form>
        <input type="text"/>
    </form>
}

templ Page(data *[]mock_data.Course) {
    @utils.Page("Courses") {
        @Content(data)
    }
}