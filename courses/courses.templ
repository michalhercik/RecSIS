package courses

import (
    "github.com/michalhercik/RecSIS/utils"
)

templ Content(data *CoursesData) {
    <form>
        <input type="text"/>
    </form>
}

templ Page(data *CoursesData) {
    @utils.Page("Courses") {
        @Content(data)
    }
}